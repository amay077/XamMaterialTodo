<?xml version="1.0" encoding="utf-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:XamMaterialTodo.Main" 
    xmlns:vc="clr-namespace:XamMaterialTodo.ValueConverters" 
    xmlns:app="clr-namespace:XamMaterialTodo" 
    x:Class="XamMaterialTodo.Main.MainPage"
    Title="TODO List"
    x:Name="page"
    Visual="Default">    
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <vc:DoneConverter x:Key="doneConv" />
            <vc:PriorityConverter x:Key="priorityConv" />
            <vc:DueDateConverter x:Key="dueDateConv" />
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ContentPage.ToolbarItems> 
        <ToolbarItem x:Name="toolBarItem" Command="{Binding ToggleShowCompletedCommand}" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>   
                    
            <ListView Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2" 
                      x:Name="listView" ItemsSource="{Binding TodoItems}" SelectionMode="None"
                      SeparatorVisibility="None"
                      ItemTapped="ListView_ItemTapped" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.ContextActions>
                                <MenuItem IsEnabled="{Binding IsCompleted}" Text="Undone"
                                          Command="{Binding BindingContext.UndoneCommand, Source={x:Reference page}}" 
                                          CommandParameter="{Binding}" />

                                <MenuItem Text="Delete" 
                                          Clicked="DeleteMenu_Clicked" 
                                          CommandParameter="{Binding}" />
                            </ViewCell.ContextActions>
                            <FlexLayout Direction="Row" JustifyContent="SpaceBetween"
                                        Padding="10,30"
                                HorizontalOptions="FillAndExpand">
                                <Image FlexLayout.AlignSelf="Center" WidthRequest="30" HeightRequest="30" Margin="0,0,5,0"
                                       Source="{Binding IsCompleted, Converter={StaticResource doneConv}}" />
                                <Label FlexLayout.AlignSelf="Center" FlexLayout.Grow="1" Text="{Binding Title}" Margin="0,0,5,0"/>
                                <Label FlexLayout.AlignSelf="Center" Text="{Binding DueDate, Converter={StaticResource dueDateConv}}" Margin="0,0,5,0" />
                                <Label FlexLayout.AlignSelf="Center" FlexLayout.Basis="50" HorizontalTextAlignment="End" Text="{Binding Priority, Converter={StaticResource priorityConv}}"/>
                            </FlexLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <ImageButton Grid.Column="1" Grid.Row="1"
                Source="https://raw.githubusercontent.com/amay077/XamMaterialTodo/master/img/baseline_add_white_48dp.png"
                BackgroundColor="#2B78FE" 
                Padding="10" CornerRadius="25" 
                WidthRequest="50" HeightRequest="50"  
                VerticalOptions="Center" HorizontalOptions="Center"
                Command="{Binding AddCommand}"/>
        
        </Grid>
    </ContentPage.Content>
    
</ContentPage>